# Copy to .env for local dev, or set in Vercel → Project → Settings → Environment Variables.
# Must point to the deployed Render backend URL (no trailing slash).
VITE_API_BASE_URL=https://kronos-swarm-core.onrender.com
